import React from 'react';
import Header from './components/Header'; import Hero from './components/Hero'; import About from './components/About'; import Experience from './components/Experience'; import Projects from './components/Projects'; import Skills from './components/Skills'; import Education from './components/Education'; import Contact from './components/Contact'; import Footer from './components/Footer';
import { translations } from './data/translations';
export default function App(){ const [lang,setLang]=React.useState('en'); const t=translations[lang]; React.useEffect(()=>{document.documentElement.lang=lang==='pt'?'pt-BR':'en';document.title=t.page_title; const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)document.querySelectorAll('.navlinks button,.mobile-drawer>button').forEach(b=>b.classList.toggle('active',b.dataset.target===e.target.id));}),{rootMargin:'-25% 0px -60% 0px'}); ['hero','sobre','experiencia','projetos','skills','formacao','contato'].forEach(id=>{const e=document.getElementById(id);if(e)io.observe(e)}); const rev=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');rev.unobserve(e.target)}}),{threshold:.12}); document.querySelectorAll('.reveal').forEach(e=>rev.observe(e)); return ()=>{io.disconnect();rev.disconnect()}},[lang]); React.useEffect(()=>{const onScroll=()=>{const h=document.documentElement;const pct=h.scrollTop/(h.scrollHeight-h.clientHeight)*100;document.getElementById('progressBar').style.width=`${pct}%`;document.getElementById('floatingBtn').classList.toggle('visible',scrollY>350)};window.addEventListener('scroll',onScroll,{passive:true});return()=>window.removeEventListener('scroll',onScroll)},[]); const navigate=id=>document.getElementById(id)?.scrollIntoView({behavior:'smooth'}); return <><div className="tech-grid" aria-hidden="true"></div><div className="tech-grid-glow" id="techGridGlow" aria-hidden="true"></div><div className="progress-bar" id="progressBar" aria-hidden="true"></div><Header t={t} lang={lang} setLang={setLang} onNavigate={navigate}/><main><Hero t={t} onNavigate={navigate}/><About t={t}/><Experience t={t}/><Projects t={t}/><Skills t={t}/><Education t={t}/><Contact t={t}/></main><Footer t={t}/><button className="floating-btn" id="floatingBtn" type="button" aria-label="Back to top" onClick={()=>navigate('hero')}><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5"><path d="M12 19V5M5 12l7-7 7 7"/></svg></button></> }
